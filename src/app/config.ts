// src/config.ts

// Switch between "mock" and "live"
// export const API_MODE: "mock" | "live" = "mock";  // Change to "live" to use real API endpoints
export const API_MODE: "mock" | "live" = "live";  // Base URL for live API (if needed, can also be set in api.ts)
